[package]
name = "gcode"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
publish = false
description = "G-code lexer for the kalico motion planner. Lexically G-code-aware, motion-semantics-agnostic."
license.workspace = true

[dependencies]
thiserror = { workspace = true }

[dev-dependencies]
proptest = "1.5"

[lints]
workspace = true
