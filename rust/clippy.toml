disallowed-methods = [
    { path = "f64::sin", reason = "OS libm differs per platform; use libm::sin for bit-reproducible trajectories" },
    { path = "f64::cos", reason = "OS libm differs per platform; use libm::cos" },
    { path = "f64::sin_cos", reason = "OS libm differs per platform; use libm::sincos" },
    { path = "f64::tan", reason = "OS libm differs per platform; use libm::tan" },
    { path = "f64::asin", reason = "OS libm differs per platform; use libm::asin" },
    { path = "f64::acos", reason = "OS libm differs per platform; use libm::acos" },
    { path = "f64::atan", reason = "OS libm differs per platform; use libm::atan" },
    { path = "f64::atan2", reason = "OS libm differs per platform; use libm::atan2" },
    { path = "f64::exp", reason = "OS libm differs per platform; use libm::exp" },
    { path = "f64::exp2", reason = "OS libm differs per platform; use libm::exp2" },
    { path = "f64::exp_m1", reason = "OS libm differs per platform; use libm::expm1" },
    { path = "f64::ln", reason = "OS libm differs per platform; use libm::log" },
    { path = "f64::ln_1p", reason = "OS libm differs per platform; use libm::log1p" },
    { path = "f64::log10", reason = "OS libm differs per platform; use libm::log10" },
    { path = "f64::log2", reason = "OS libm differs per platform; use libm::log2" },
    { path = "f64::powf", reason = "OS libm differs per platform; use libm::pow" },
    { path = "f64::cbrt", reason = "OS libm differs per platform; use libm::cbrt" },
    { path = "f64::hypot", reason = "OS libm differs per platform; use libm::hypot" },
    { path = "f64::sinh", reason = "OS libm differs per platform; use libm::sinh" },
    { path = "f64::cosh", reason = "OS libm differs per platform; use libm::cosh" },
    { path = "f64::tanh", reason = "OS libm differs per platform; use libm::tanh" },
]
