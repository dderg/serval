[package]
name = "step-shim"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
publish = false
description = "Host-side stepcompress shim: clocked motor spans -> step roots -> queue_step frames."
license.workspace = true

[dependencies]
trajectory = { path = "../trajectory" }
libm = { version = "0.2", default-features = false }

[lints]
workspace = true
