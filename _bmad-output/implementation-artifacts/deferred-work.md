# Deferred Work

Findings surfaced during review that are real but out of the originating story's scope.

## From spec-motion-1-typed-segment-ir (review 2026-06-18)

- **Anchor-data finiteness validation (step-2 lowering).** `Arc::origin` / `Arc::start_angle` and `Clothoid::start_pose` are accepted without `is_finite` checks, and `Line::try_new` rejects only `len == 0.0` (a NaN coordinate yields `len = NaN`, which is neither `== 0.0` nor `> 0.0`, so it slips through). This does not affect the step-1 κ-space contract — none of those fields feed any `CurvatureProfile` method, and the spec explicitly stores anchors as inert data evaluated only at lowering. Validate anchor finiteness where position is first evaluated (step 2), or fold it into the lowering constructor. **(RESOLVED in step-2 lowering: `lower_constant_speed` rejects non-finite Line start/end, Arc origin+start_angle, and Clothoid start_pose with `InvalidLowering`; paired test `non_finite_anchor_rejected`.)**

## From spec-motion-2-execution-lowering (review 2026-06-18)

- **Small-σ Fresnel precision cliff.** `clothoid_offset`'s σ=0 closed form fires only at exactly `sigma == 0.0`; for tiny nonzero σ the completion-of-square produces large Fresnel arguments whose differenced ~0.5-magnitude values cancel (error grows to ~3e-9 at σ=1e-9). Sub-physical (picometer-scale) and degenerate-arc-like, so not patched now. Add a tolerance-band fallback to the arc/line limit if the fitter ever emits near-arc clothoids.
- **`LoweredSample.followers` drops `axis_index`.** Follower samples are a positional `Vec<f64>` of `ratio·s` in `seg.followers` order; `FollowerDemand.axis_index` is not carried. Fine for observability (zip with `seg.followers`), but revisit the struct shape when a real follower consumer (front-end, step 3+) needs the axis mapping.

- **Signed-curvature convention (step-2 heading).** `Arc::kappa` returns `+1/radius` regardless of sweep sign, while `Clothoid::kappa` is signed (`κ₀ + σs`). The velocity cap consumes `|κ|` via `kappa_peak`, so step 1 is correct. If a downstream consumer needs signed curvature to distinguish turn direction (heading integration), settle the sign convention when that consumer lands.

## From spec-motion-3-frontend-gcode (review 2026-06-18)

- **Tiny-segment follower-ratio blow-up (extruder velocity spike).** A near-zero-length arc/line (`arc_length`/`spatial_distance` just above the `1e-9` displacement epsilon) carrying a real extrusion delta produces a follower `ratio = e_delta / length` of thousands — a micron-scale path commanding mm-scale extruder motion. The `1e-9` epsilon gates extrusion and spatial displacement independently but nothing bounds the resulting ratio. Sub-physical for representative slicer output and identical in shape to the existing `motion-engine::classify_and_build`, so not patched in the frontend. The principled fix is an **extruder-velocity / volumetric-flow cap in the velocity planner** (step 5+, see architecture "Volumetric-flow cap" / PA-augmented transient) — geometry has no velocity limit to check against. Revisit when that cap lands.

## Deferred from: code review of spec-motion-4-corner-fitter (2026-06-18)

- **Near-`theta_max` corner blends produce extreme κ_peak and inflated follower ratios (same class as the step-3 follower blow-up).** A corner approaching the `theta_max` bound (default `π − 1e-3`, i.e. ~179.94°) still blends, producing a valid but near-singular biclothoid: `κ_peak ~1e5–1e6 /mm` (radius ~10 nm), `σ ~1e10`, and a follower ratio inflated ~877× at the bound (unbounded as θ→π). The fitter conserves ΔE exactly and all `Clothoid::try_new` checks pass — the geometry is correct — but the velocity planner receives a near-singular curvature and a large follower ratio with no upper-bound check (`validate_followers` enforces only finite-and-nonzero). This is the **same defect class** as the tiny-segment follower-ratio blow-up above: geometry has no velocity/flow limit to check against. The principled fix is the **extruder-velocity / volumetric-flow cap in the velocity planner** (step 5+), which will cap the corner velocity (`v = √(a/κ_peak)` → near-stop, correct for a near-reversal) and the PA-augmented flow transient. Revisit when that cap lands. (`theta_max` is frozen in the step-4 spec; whether to also tighten it to leave more near-reversal corners sharp is a separate tuning question to settle once the velocity planner exists.)

## Deferred from: code review of spec-motion-8-chain-fit (2026-06-18)

- **Greedy `detect_runs` never backtracks the run end.** On a `reconstruct` abandonment for the maximal candidate `[i..=end]`, the scanner advances the start (`i += 1`) and regrows, so it only ever tries co-circular *suffixes*, never the shorter *prefix* `[i..=end-1]`. A single non-co-circular trailing facet therefore prevents reconstruction of an entire long, otherwise-co-circular arc (it falls back to the per-corner sawtooth) — a throughput regression on "mostly clean with one blemish" runs. Uniform arcs (the dominant #4228 case) reconstruct in one shot, so V1 ships without backtracking; add an inner shrink-the-end retry when a sub-maximal reconstruction would beat the per-corner fallback. (`fitter/chain.rs::detect_runs`)
- **No observability when a detected run abandons reconstruction.** When a run passes detection (same-turn, coplanar, ≥`min_run_junctions`) but `reconstruct` returns `None` (residual > `cocircular_tol`, seam miss, budget/shift failure, or vertex-tube miss), the span silently degrades to the per-corner path with no counter or reason recorded in `FitReport`. A single anomalous facet's limits (tiny δ ⇒ `l_t` underflow) nukes a long run with no diagnostic. Add an abandoned-chain counter / reason enum to `FitReport` for tuning visibility. (`fitter/chain.rs`)
## Deferred from: code review of spec-motion-9-profile-lowering (2026-06-18)

- **`lower_profile` sample count is unbounded for absurdly-tiny-positive velocities.** The per-interval guard rejects `v_sum <= 0` but admits arbitrarily small positive velocities (e.g. both endpoints `~1e-12`), so the interval duration `2·Δs/v_sum` and hence the global fixed-rate sample count `n` can grow enormous-but-finite — below the `count + 1 >= usize::MAX` addressable-range guard, so `Vec::with_capacity(n+1)` OOM-aborts rather than returning a clean `InvalidLowering`. Not reachable from the real `plan_velocity` producer (its velocity epsilon is `1e-9`, and the forward/backward reach from any rest anchor under positive accel is strictly bounded below), and identical in shape to the pre-existing `lower_constant_speed` count guard (`path/lowering.rs`). The principled fix is a deliberate max-samples-per-trajectory cap (or a minimum-traversal-velocity floor) returning `InvalidLowering`, applied consistently to both lowering entry points — a value to pin, not guess. (`rust/geometry/src/execution.rs::lower_profile`)

- **`solve3` tests hard-singularity (`|det| < 1e-18`), not conditioning.** The incircle normal-equations solve guards only an exactly-singular matrix; a near-degenerate (near-straight, huge-radius) run yields a finite but ill-conditioned `(O, ρ)`. In practice the downstream `cocircular_tol` residual gate, the `rho > BUDGET_EPS` check, and the `delta_sweep > ANGLE_EPS` gate backstop it (a near-straight run has `theta_run ≈ 0 ⇒ delta_sweep ≤ 0 ⇒ abandon`), so no incorrect output reaches the planner — but the intended singular-matrix guard is not doing the conditioning job. Switch to a condition-number / normalized-pivot test if a near-straight faceted run ever needs explicit rejection. (`fitter/chain.rs::solve3`)

## From spec-wire-square-corner-velocity-from-printer (review 2026-06-19)

- **`path_velocity_limits()` still hardcodes the SCV default.** `rust/motion-engine/src/config.rs` `PlannerConfig::path_velocity_limits()` calls `geometry::VelocityLimits::try_new(max_v, max_a, DEFAULT_SQUARE_CORNER_VELOCITY_MM_S)` rather than `self.cartesian.square_corner_velocity`. After this change the two live SCV consumers (stream init + `submit_move`) read the configured value, but this method would silently revert to 5.0. It is currently **dead code — zero callers** (verified by workspace grep), so no runtime impact, and the originating spec explicitly scoped it out (`Never: do not touch the dead path_velocity_limits() path`). Route it through `self.cartesian.square_corner_velocity` or delete the method if/when it is resurrected.

## From spec-motion-pipeline-viz (review 2026-06-19)

- **G91 relative positioning not handled.** `scripts/viz_pipeline.py` parser assumes G90 absolute mode. If G-code uses G91, coordinates are interpreted as absolute, producing wrong geometry. Slicer output is almost always G90 so low-priority. Detect G91 and fail loudly.
- **Opaque error messages.** Rust-side errors surface as `Debug` format (`InvalidFeedrate { line_no: 42 }`) rather than user-friendly text. Track source G-code line numbers through the parser and use `Display` formatting.
- **Large file performance.** No `--max-moves` flag or subsampling. Files with 100k+ moves produce ~1M sample points, slow matplotlib render and large PNGs. Add optional move limit and point downsampling.

## From spec-motion-12 T3 (found 2026-06-21, post-merge of PR #88)

- **Cruise on/off-ramp tangential jerk gap (next motion work, ~Motion-14).** `a_t` is jerk-limited on the way up (jerk-up from rest) but **steps `a_max → 0` discontinuously when `v` reaches `max_velocity`** (and `0 → −a_max` leaving cruise). The forward scurve primitive (`velocity/scurve.rs` `breakpoints`/`accel_at`) only models jerk-up + hold, with no jerk-down to land on cruise at `a=0`; `disk.rs` `forward_branch` returns `a=0` at the flat ceiling so the transition steps, and `build_run_bridge` only catches sign-flip crossovers (not `+a→0`). Hits every move reaching `max_velocity`. Not exercised by demo4 (sub-cruise) but exposed by Neptune `test2.gcode` (`v100 a1000 jerk1e6 scv30`). Fully diagnosed + reproduced in **`investigations/cruise-onset-jerk-limit-handoff.md`** (fix: add jerk-down-to-ceiling phase / bridge the `+a→0` crossover). Also notes the related near-stop corner-apex valley (tiny) and the lateral G3 seam jerk (Non-Goal, fitter's job). **Now spec'd → `spec-motion-14-cruise-onset-jerk-limit.md`** (status: in-progress).

## From spec-motion-14 T2 (found 2026-06-21)

- **Jerk-aware peak estimation in the forward/backward sweep.** The base velocity sweep (`velocity.rs` / `disk.rs` forward/backward branches) marks a move as reaching `flat_ceiling` using disk/scurve reachability that ignores the jerk-down distance needed to *land* on the ceiling at `a=0`. For a straight shorter than `~2×` the jerk-limited accel distance, the reported peak (`v_max`) is therefore **not jerk-feasible** — the move's true peak is below `v_max`, and the Motion-14 entry/exit roll-offs overlap. Motion-14 detects this overlap and falls back to the base profile (keeping its cruise-touch step) instead of emitting interleaved arcs (`disk.rs::reconstruct_flat`, guarded by `cruise_onset.rs::short_overlap_move_falls_back_to_base_without_garbage`). Only reachable at low jerk — realistic configs (Neptune `j=1e6`) have sub-millimetre roll-offs and never overlap — so low priority. Proper fix: make the sweep's peak estimate account for the jerk-limited up+down ramp so the reported peak is feasible (then a short move peaks below `v_max` and is handled by the existing sign-flip apex bridge, no special case). See spec-motion-14 R4.

## From spec-arc-fit-config-section (review 2026-06-19)

Pre-existing / hardening items surfaced while adding the opt-in `[arc_fit]` gates; not caused by that change:

- **Leading-collinear run never reconstructs.** `fitter/chain.rs::reconstruct` anchors its plane on `turn_normal(t0, lines[1])`; if a run begins with a collinear pair (`grow_run` advances past a `theta<=theta_min` first junction), those headings are parallel → `turn_normal` returns `None` → the whole run is dropped to per-corner. Pre-existing; the new facet-length gate makes leading-collinear runs slightly more reachable. Fix: anchor the plane on the first genuinely-turning junction.
- **O(n²) re-growth on gate-split runs.** When `grow_run` breaks on the new length/angle gate below `min_run_junctions`, `detect_runs` falls to `i += 1` and re-grows over largely the same facets. Pathological faceting can make this quadratic. Consider advancing `i` past the inspected span on a sub-threshold break.
- **`ChainFitConfig::with_arc_fit` / `ArcFitConfig` are not self-validating.** Production validates knobs at the FFI boundary (`bridge.rs` + klippy `getfloat`), and the rest of the config types follow the same validate-at-boundary pattern, so internal callers are trusted. The test helper deliberately passes `f64::INFINITY`/`PI` as an "ungated" sentinel. If a future non-boundary Rust caller appears, add a `try_*` constructor rather than validating in `with_arc_fit` (which would break the sentinel idiom).
- **No automated test for the bad-param config error.** `max_angle_deg`/`facet_length_mm` non-positive (and now `max_angle_deg >= 180`) are rejected by klippy `getfloat(above=0.0, below=180.0)` and a bridge `PyValueError` backstop — verified by inspection, but no regression test exercises either error path. Add a klippy config-load test asserting the error fires on `facet_length_mm: 0` and `max_angle_deg: 200`.

## Pump-backlog backpressure — follow-ups (from spec-host-pump-backpressure review, 2026-06-21)

- **Reset the pump backlog atomic to 0 on every `run_pump` exit path** (Shutdown / `RecvError` disconnect / fatal transport). Today the atomic keeps its last value on exit. Currently unreachable in practice (fatal transport aborts the process; other exits are teardown), so deferred as hardening — but a stale high value could mask a real fault if a future recovery path keeps the engine alive after the pump dies.
- **Route a genuine homing-active signal into `_check_pause`.** The `_drip_active` guard only covers `drip_move`, which currently has no callers; homing issues retract/backoff/settle via `toolhead.move()`/`dwell()` directly (`klippy/extras/homing.py`). In practice homing pre-drains and runs low backlog so `_check_pause` early-returns, but the "never pause during homing" intent is not structurally enforced. If a homing/drip path that accumulates backlog is added, thread a homing flag into `_check_pause`.
