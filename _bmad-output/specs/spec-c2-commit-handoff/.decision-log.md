# Decision log — spec-c2-commit-handoff

## 2026-06-30 — create

Input: rich technical directive (curvature-continuous commit handoff) plus the full investigation context that produced it — the streaming planner commits only at κ=0 seams because the window handoff carries a scalar entry velocity; the arc-ease quality fix is geometrically validated on the whole-buffer fit but drifts at small commit caps. Mode: interactive, rich input — extracted directly, no elicitation.

Routing: kernel holds Why + 3 capabilities + constraints/non-goals/success. The current commit-path mechanism (commit loop, `is_clean_seam` κ=0 rule, finality barrier, `head_len_restore` precedent, warm-start κ=0 assumption, `pump.rs` fatal guard, regression anchor) is multi-item load-bearing reference for the implementer → `brownfield.md` companion.

Verdict — Pass 1 (coherence): all three capabilities carry intent + success; intents are WHAT (commit mid-arc, resume at matched κ, warm-start from carried κ) with HOW detail pushed to brownfield; the five constraints each rule something out (no silent padding, preserve append-finality, no κ=0-only / no arc-dropping, host-side only, state in `StreamState`); four non-goals; success signal is the named regression at named caps plus a window-invariance κ assertion. Two inferred calls became `assumptions[]` (curvature is the highest derivative carried; pump-guard tolerance governs the contract); two genuine implementation forks became `open_questions[]` (how much arc identity to carry; entry-κ as hard cap vs re-derived). Pass 2 (preservation): every load-bearing source claim — the contract, the scalar-handoff root cause, the three implementation pieces, the unblocked quality fix, the regression anchor — landed in SPEC.md or brownfield.md. No wrapper-only content dropped silently.

## 2026-06-30 — update: open questions resolved

Both open questions closed by the user. OQ1 (how much arc state to carry): the carried boundary is just the G2 endpoint — position, tangent, signed curvature (a curvature vector, carrying its bending plane). The osculating circle is derivable (centre = P + N/κ), so no arc identity or clothoid σ is carried; the same state serves arc and clothoid resume uniformly, and the resume fitter reads (tangent, κ) and fits its continuation forward freely. OQ2 (entry-κ hard cap vs re-derived): the warm-start consumes the carried entry curvature as a fixed boundary condition, like entry velocity — drift-free. CAP-2, the `StreamState` constraint, and Assumptions updated accordingly; Open Questions section removed (empty).
