# Decision log — spec-motion-pipeline-rewrite

## 2026-06-17 — create

**Source:** `docs/rewrite/motion-pipeline-architectural-map.md` (pre-existing brain-dump / architecture map, user-authored, mixed WHAT + HOW). Interactive mode, dderg.

**Sorting decision.** The map was well-organized but mixed kernel-level WHAT (north star, scope, success, open questions) with settled HOW (IR contract, design/execution split, constraint model, corners/blends, 3D embedding, build sequence). Lifted the WHAT into the five-field kernel. Rather than duplicate the deep HOW into a spec-authored companion, **adopted the map itself as a companion** (`../../../docs/rewrite/motion-pipeline-architectural-map.md`) — it remains user-owned and holds the architecture reference, build sequence, and narrative rationale downstream reads. Added one spec-authored companion `architecture-diagrams.md` (clean mermaid of the pipeline + type-state enrichment + shaper-module duality), since diagrams always live in a companion and the source held only ASCII.

**Kernel.** 6 capabilities (CAP-1 front-end, CAP-2 TOPP, CAP-3 shaper-aware blend, CAP-4 execution, CAP-5 3D/mesh, CAP-6 shaper/PA consistency). 12 constraints, 3 non-goals, 1 success signal, 2 assumptions, 7 open questions (lifted verbatim-in-intent from the map's "Open — to be discovered").

**Self-validate verdict.**
- *Pass 1 (coherence, rules 1–6, 8):* PASS. Every capability carries intent + success; constraints each rule something out; non-goals explicit (3); success signal is testable (measurably-faster trajectory, no added ringing, measurable gain over the accel-only skeleton); CAP IDs unique and stable; prose lean. CAP-6's intent names "per-axis execution post-processors" — mildly HOW-flavored but load-bearing placement that is also stated as a constraint, so retained deliberately.
- *Pass 2 (preservation, rule 7):* PASS. The architectural map is an adopted companion, so all its load-bearing claims are preserved by reference; the WHAT subset is additionally distilled into the kernel. No wrapper-only drops; no load-bearing claim lost. `sources:` is empty because the map retains downstream value (the HOW) and is therefore a companion, not an absorbed source.

**Open items carried:** the 7 open questions are unresolved by design — they mirror the map's open section and need human decisions before the relevant downstream work (architecture, TOPP, execution lowering) commits.

## 2026-06-17 — update (scope reframe from dderg)

**Change signal.** dderg confirmed Assumption 1 (MCU/execution stays — works well) and corrected Assumption 2: whole-print "representative slicer output" is **not** the V1 benchmark, because faster-than-real-time whole-print simulation does not exist yet. The real V1 goal is the *foundation* — a pipeline whose seams do not prevent the individual pieces from later being tuned to perfect performance. The foundation's job is to not get in the way; per-piece tuning to ultimate speed is downstream.

**Edits.**
- *Why* — reframed: ultimate payoff is still minimum print time, but the immediate work is the foundation that lets each piece reach its own optimum with no layer capping an adjacent one.
- *Success signal* — rewritten from "measurably faster on hardware" to foundational soundness: every interface independently testable; no layer caps downstream optimality; tightening any one pass flows through without touching adjacent layers.
- *CAP-2 success* — "beats mainline on representative slicer output" → "no slower than current planner on test/recorded sequences, checkable offline."
- *CAP-3 success* — "measurably higher max_accel on active hardware" → blend machinery emits a shaper-cancellable bump within δ on a corner test, rung independently tunable (seam-supports-tuning, not a hardware number).
- *Non-goals* — added: per-piece performance tuning to ultimate speed and whole-print benchmarking are out of scope; this spec builds the enabling foundation, not the optimum.
- *Assumptions* — #1 annotated "confirmed, works well"; #2 replaced with the no-real-time-whole-print-sim reality and per-interface/recorded-sequence validation.

**Self-validate verdict.** PASS both passes. Capabilities still carry intent+success and stay foundation-testable offline; success signal is concrete (walking skeleton end-to-end + improvement-flows-through demonstration); non-goals now explicitly fence the ultimate-speed tuning out. Preservation intact — no claim dropped; the reframe narrows the bar without losing the north-star (minimum print time remains the stated ultimate driver in Why).

## 2026-06-17 — design discussion, consolidated (the big one)

Worked through the "settled" claims one by one with dderg, pressure-testing each. The verbatim brain-dump companion was deleted earlier at dderg's request ("discuss, then formalize"); this entry records what we actually decided, now written into the rewritten `SPEC.md` and the new authored `architecture.md` companion. Two Explore agents read the current Rust tree (`rust/`) to ground the discussion in reality.

**What the code investigation changed.** The extruder is already a follower (`FollowerDemand`, `try_new_virtual`, odometer over the post-shaping signal) — my "extruder breaks κ(s)" objection was moot. The current TOPP is *not* "TOPP on raw Bézier-t" as the map claimed — it reparameterizes to arc length (1024-pt table) and solves a curvature-based convex SOCP, then SLP9 jerk refinement, then iterative joining. So this is a restructuring, and the dominant non-real-time cost is **jerk-in-the-convex-core (SLP9)**, not the representation.

**Decisions reached:**
- Representation: typed `Segment { Line | Arc | Clothoid }` + follower channel. κ is a *derived* field, not the generative truth (typed segments are). Justification for curvature = "it's the coordinate the constraint is written in," not the (planar, oversold) fundamental theorem. Clothoid: κ(s) closed-form (design space), Fresnel only at execution lowering.
- Blend shape decided: **clothoid**. The accel vector rides the limit at magnitude a_max and rotates (braking→centripetal→forward) — vector-budget trading is why it beats an arc on time; lateral jerk is bounded by the geometry for free.
- Fitter is **shaper-agnostic by authority** (not a bet/approximation). Planner never caps accel for ringing; max_accel is the user's sovereign knob. This *reverses* the map's "shaper-aware middleware" and drops the post-shaper-within-δ goal (now a decided, recoverable non-goal). Fitter inputs: δ bounds geometry, max_accel decides blend-or-not + speed.
- Jerk split: lateral = free (clothoid geometry); tangential = **1-D S-curve in the velocity lookahead, not a post-processor** (the no-room accel→decel reversal kills the post-processor). Jerk lives in s(t), never geometry → no artifacts; fail loudly if a segment can't absorb its smoothing.
- Velocity planning: decoupling lateral (geometry caps) from tangential (1-D) **removes the SOCP** in favor of forward-backward integration + S-curve ramps over closed-form clothoid caps. The velocity profile over a clothoid is an *output* of the global sweep (all-decel / all-accel / dip / cruise). Tighter-next-feature = ordinary backward propagation; coupling horizon = window, reset at full stops.
- Acceleration continuity = fitter guarantees κ-continuity (lateral) + shared boundary node (tangential); fitter guarantees collinearity except move starts.
- SCV reused as the knob, angle dependence from closed-form `κ_peak(angle, δ)`.
- Shaper/PA stay execution post-processors; follower advances on post-shaper velocity (already coded). PA-profile simplification = TODO after basic movement.
- Build: constant-speed-per-feature skeleton first, then limit-riding through clothoids as the measurable upgrade.

**Open questions now tracked in SPEC.md:** jerk-limit floor; `κ_peak(angle, δ)`; **forward-backward sweep convergence/real-time (the last unverified load-bearing claim)**; ride-limit-vs-cap switch-on; execution lowering (Fresnel degree/tolerance); dumb-gcode chain detection; back-to-back-clothoid dκ/ds step; helical G2/G3 support; extruder post-IS simplification.

**Carried, NOT pressure-tested (provisional):** convex-body shape, 3D planar pairwise blend, bed-mesh-Z dependent axis. Flagged as such in both SPEC.md and architecture.md so they're not mistaken for vetted decisions.

**Self-validate verdict.** PASS. Kernel rewritten: CAP-3 de-shaper-ized (clothoid blend, set by δ+max_accel), CAP-6 reframed (post-processors + follower-on-shaped-velocity, dropped the post-shaper-δ success criterion), constraints updated (shaper-agnostic-by-authority, jerk-in-s(t), forward-backward not SOCP, clothoid/Fresnel, collinearity), non-goals gained the shaper-aware-fitter and helical-arc entries. Preservation: the deep HOW now lives in the authored `architecture.md` (a spec-authored companion this time, earned through discussion — not the verbatim paste). The "Corrections to the original map" section preserves the rationale for every reversal so the reasoning isn't lost.
