# Decision log — seam-continuity-test-platform

## 2026-06-24 — create (from chat brain dump + junction investigation)

**Pass 1 (coherence).** All 5 capabilities carry intent+success; constraints each rule something out (real StreamState vs mock; forced-commit coverage; determinism; production-matching semantics; shrinking); 4 non-goals; success signal is testable (caught offline before bench). CAP-2/CAP-3 name production mechanisms (`commit(force=true)`, the production check's compared quantities) — judged load-bearing reference, not HOW-prescription, since the platform's value is testing exactly that production code. Verdict: passes 1–6, 8.

**Pass 2 (preservation).** Every load-bearing input claim landed: generalize `repro_junction` (Why/CAP-1); arbitrary gcode through real `StreamState` (CAP-1); swept/random commit cadence + forced commits (CAP-2); the forced-commit-then-replan crux the user raised (CAP-2 + Constraint 2); multi-order continuity asserts (CAP-3); pin the 0.1545 mm cube regression (CAP-4); fuzz + shrink (CAP-5 + Constraint 5); reusable-not-one-off (Why/Success signal); deterministic repro facts cap≤24/≥32 (CAP-1/2 success). Non-goals captured the three exclusions (fix the bug, HIL, print sim) plus an added one (pump/backpressure direct testing). Added one claim not explicit in input but load-bearing: "harness pass ⇒ bench won't panic" (Constraint 4) — the trustworthiness invariant. No wrapper-only drops.

**Open questions (need a human call before/at implementation):** (1) assert at ShapedSegment level (cheap, catches this bug) vs lowered PieceEntry level (fully satisfies the no-panic constraint, costs the `enqueue`+`project` wiring) for v1; (2) generated-random-gcode fuzzing in v1 vs schedule-fuzzing over a real-gcode corpus first.

**Assumptions logged:** schedule-fuzzing prioritized over gcode-generation; C0 mandatory, C1/curvature best-effort.

## 2026-06-24 — update: expected-failure framing

User clarified the expected result is **many failing tests**, not a green suite — the platform is a measurement instrument for an unfixed, widespread bug. Added CAP-6 (expected-failure ledger: baseline the failing-case set, gate CI on *new* failures/worsened magnitude, shrink monotonically) and rewrote the Success signal so the win is a tracked failing-count falling to zero, not an immediately-green suite. Preservation: the new claim lands in Success signal + CAP-6; coherence holds (CAP-6 carries intent+success; no ID reuse). No other fields touched.

## 2026-06-24 — resolve open question #1: piece-level from the start

User chose piece-level assertions over ShapedSegment-level for v1. CAP-3 now mandates lowering through the real `enqueue` path (+ deterministic `project` closure) and comparing the production quantities (prev `coeffs[3]` vs next `coeffs[0]`), closing the "harness pass ⇒ bench won't panic" guarantee with no ShapedSegment-only blind spot. Open question #1 removed; one open question remains (generated-gcode fuzzing in v1).

## 2026-06-24 — resolve open question #2: schedule fuzzing for v1

User chose schedule-fuzzing over a curated real-gcode corpus for v1; generated/synthetic gcode geometry deferred. CAP-5 scoped to schedule-fuzzing (random caps + forced-commit points) over real gcode; generated geometry added to Non-goals as a fast-follow. The prior prioritization assumption is now confirmed and removed from Assumptions (folded into CAP-5 + Non-goal). No open questions remain — spec fully settled.

## 2026-06-24 — correction: embrace the red, no suppression

User rejected the CAP-6 expected-failure-ledger/green-gating framing: failing seam cases must FAIL (red), never be skipped/`#[ignore]`d/xfail'd/baselined. Reframed CAP-6 to "surface every failing seam as a real red test, count visible, shrinks as fixes land"; added a Constraint forbidding any expected-failure/quarantine mechanism (mirrors the project fail-loud mandate); rewrote the Success signal to "a red board we watch turn green." This aligns with un-ignoring `junction_position_discontinuity_is_fatal` earlier. No green-by-suppression anywhere in the contract.
