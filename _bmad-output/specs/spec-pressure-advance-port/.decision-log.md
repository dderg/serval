# Decision log — spec-pressure-advance-port

## 2026-06-22 — Create from brainstorming session

Source: `_bmad-output/brainstorming/brainstorming-session-2026-06-22-0901.md` (rich, pre-sorted by the session). Mode: express distill, no elicitation needed — input carried decided answers to all five kernel fields.

### Key routing decisions
- Five-field kernel in SPEC.md; the file:line hook map, gain-kernel math justification, and test anchors routed to companion `code-map.md` (multi-item reference catalog — would bloat the kernel).
- "Where PA lives" option space (5 options): front-runner → CAP-1; rejected plan-time follower constraint → Non-goal; the rest folded into the placement decision.
- Dead `topp` coupling deletion expressed as a **Constraint** (decoupling cannot regress), not a capability, since it is an invariant-protection task, not a user/system-facing ability.
- The two 🟡 verification items became `open_questions` (PA×shaping order; MCU step-rate ceiling). The resolved 🔴 (`apply_derivative_gain` real/correct/tested) became `assumptions` + code-map fact, shrinking CAP-1 to wiring confirmation.

### Self-validate verdict
Pass 1 (coherence, rules 1–6, 8): all four capabilities carry intent+success; constraints each rule something out (post-processors-never-feed-limits, no-extruder-limit-on-XYZ, verbatim mainline field reuse, mandatory deletion of dead coupling, fail-loud); four explicit non-goals; success signal is demonstrable (PA-on vs PA-off timing identity + extrude-only limit obeyed + topp code deleted + suite green). CAP-1 intent carries the defining `e(t)+=k·ė(t)` formula and "emit-time" placement — borderline HOW, but load-bearing as PA's definition and mirrored in Constraints/code-map; kept. Fixed one cross-file inconsistency (code-map labeled the deletion CAP-5; corrected to Constraint).

Pass 2 (preservation): every load-bearing source claim landed — PA first principle, live-solver=multi/topp-dead, coupling-only-in-topp, input-shaping-already-on-rail, Q1 accept-over-accel, Q2 minimal extrude-only with verbatim field names, Q3 defer tanh/smooth_time, smooth_time-redundant rationale, model seam, two extruder regimes, the full action path, and both yellow risks. Wrapper-only content dropped (brainstorming technique names, phase scaffolding, facilitation notes) — process metadata, no downstream value.

Verdict: coherent and preservation-complete. Two open questions are genuine human/hardware decisions, not distillation gaps.

## 2026-06-22 — Open questions resolved

- Post-processor application order: defined by `[axis] post_processors:` declaration order, type-agnostic — PA and input shaping are not special-cased. Folded into Constraints and CAP-4 success. Open question removed.
- MCU step-rate ceiling under a large-`k` PA spike: declared out of scope ("not our concern" — host emits, MCU executes). Added as a Non-goal. Open question removed.

Both resolutions also appended to the source brainstorming session doc. Open Questions section now empty and removed from SPEC.md.
