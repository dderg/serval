# Decision log — spec-xdirect-buzz-phase-scheduling

## 2026-06-22 — create

Source: this session's design conversation (diagnosis of the screeching/aliasing
XDIRECT buzz + the agreed phase-locked fix). No prior SPEC at this folder; create.

**Pass 1 — Coherence (Spec Law 1–6, 8).** All six capabilities carry both intent
and success. CAP-2/CAP-3 successes reference carrier phase and `N % 4` — this is
deliberately close to mechanism because the scheduling discipline *is* the
contract here (the spec was requested specifically to stop implementation drift
back to the displacement grid); the mechanism detail lives in
`scheduling-algorithm.md`, with the kernel holding the testable criterion. The
phase-scheduling constraint genuinely bends design (rules out the current
displacement grid and any fixed-Δt scheme). Five explicit non-goals. Success
signal is concrete (by-ear clean sweep + offline uniform within-cycle spacing and
N·f-only images). IDs CAP-1..6 unique.

**Pass 2 — Preservation.** Every load-bearing claim from the conversation landed:
displacement grid = root cause and non-uniform-time → in-band ZOH images + 2f
pump (Why); phase grid with rate = N·f_inst that sweeps (CAP-1/2 + constraint +
companion); N divisible by 4 pins extrema (CAP-3 + companion); N adapts to
amplitude (CAP-4); microstep/tick independence (CAP-5 + constraint); net-zero
close (CAP-6 + companion); closed-form quadratic φ-solve and the list of deleted
code — `grid_steps`/`for_rate`/`scan_dt`/`refine_extremum` branch (companion);
containment to `buzz_xdirect.rs` + the `engine.rs` call site, reuse of
`cycle_at`/unified delivery, no ISR-tick coupling (constraints). No load-bearing
claim dropped. Wrapper-only content (the /bmad-spec ceremony) carries nothing to
preserve.

Two open questions logged (N recomputation cadence; extrema alignment across an
N-change seam) and three assumptions (refill-time N with phase-continuous
anchoring; flat-top-only pinning; off-grid forced net-zero close). These are the
genuine design choices left for the implementing pass — not gaps that block it.

Verdict: coherent and preservation-complete. Ready for downstream implementation.

## 2026-06-22 — update (resolve both open questions, raise the bar to "measures clean")

Change signal from dderg: the update rate must be **exact**, not closest;
extrema must be **aligned**; overarching goal is **zero movement artefact** so the
resonance measurement is accurate — the commanded sinewave must be as clean as
possible. Both prior open questions resolve in the same direction, so they become
hard requirements rather than choices.

Edits: Why gained the "clean enough to measure" stake (accelerometer reads this
motion; sampling artefact → spurious resonance peak). CAP-2 reworded from
"tracks/≈" to **exact** `N·f_inst` (period divides evenly; `target_rate` only
bounds N). CAP-3 extended to require quarter-phase alignment across N changes, not
just flat-top pinning. Added **CAP-7**: N transitions only at carrier turning
points (velocity zero) so spacing never changes while the carrier moves. Three new
constraints (exact rate / period divides; ÷4 + grid anchored at a turning point;
N-change only at turning points). Success signal gained the no-spurious-peaks bar.
Open Questions section removed (both resolved); Assumptions trimmed to the two that
remain genuinely inferred (commanded-peak = enveloped-sine value at φ=π/2; off-grid
net-zero close). Companion N-selection section rewritten for exact rate and
turning-point transitions. Capability IDs preserved (CAP-1..6 unchanged, CAP-7 new
and never reused).

Pass 1: all seven capabilities carry intent+success; the exactness and
turning-point constraints bend design hard (they rule out per-update N drift and
any mid-segment N change). Pass 2: every claim from the change signal landed
(exact rate → CAP-2 + constraint; extrema aligned → CAP-3 + constraint;
zero-artefact goal → Why + CAP-7 + success signal). No drop. Verdict: coherent and
preservation-complete; the contract now forbids the "closest-rate" and
"mid-motion resolution switch" failure modes by construction.
