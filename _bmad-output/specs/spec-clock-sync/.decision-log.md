# Decision log — spec-clock-sync

## 2026-06-30 — create

Input: `docs/human-spec/clock-sync.md` (human-authored design intent). Mode: express distill (input was rich enough — no guided five-field walk). Grounded load-bearing technical anchors against the codebase before writing (the `-308` fault path, the host-side stale-`print_time` guard, the current shift/re-anchor mechanism, the existing per-MCU frequency/drift estimator, and the mainline heater clocksync).

**Pass 1 — Coherence (Spec Law 1–6, 8).** All three capabilities carry both intent and success. CAP-1's intent names the time-multiplier mechanism, which edges toward HOW, but the multiplier is the defining feature the input asked for and its success criterion is behavioral/testable, so it stays. Constraints each rule something out (immediate fail-loud decoupled from delayed effect; reuse-not-rebuild the estimator; supersede routine re-anchor; host-side only; bounded per-MCU). Four explicit non-goals. Success signal is demonstrable on a two-MCU bench/sim run. IDs unique and stable. Verdict: coherent.

**Pass 2 — Preservation.** Walked the source claim by claim: clock drift (jitter vs steady rate difference) → Why; current shift-start-by-drift solution and its gap/overlap/`-308` failure → Why; proposed per-MCU timeMultiplier on pushed pieces, stored and updated at sync → CAP-1; gain care / no hunting / meaningful correction → CAP-2; fail-loud on un-syncable drift → CAP-3; delayed-effect-but-immediate-fail-loud → Constraint + CAP-3 + Assumption; keep heater clocksync identical to mainline → Non-goal; PC8 stale-`print_time` homing-after-idle bug ("might be related") → Non-goal + Open Question. Every load-bearing claim landed. No wrapper-only drops.

Three open questions surfaced (PC8 bug relatedness/scope; the correctable-authority bound + gain value; open- vs closed-loop effect verification) and three assumptions logged (host-side application / no new MCU state; new distinct fault code vs reusing the clock-sync-quality family rather than overloading `-308`; open-loop command-time bound check for the fail-loud decision).

## 2026-06-30 — update (user answers)

dderg confirmed the model and resolved two of three open questions:

- **Mechanism settled.** Per-MCU stored host↔MCU **offset** + a new **speed ratio**. Offset may be re-established while idle; ratio is tuned continuously. Folded into Why and a new constraint (two correction levers).
- **New CAP-4 — homing correctness.** Endstop trigger depends on the time it fired, so the trigger timestamp must round-trip through the same offset+ratio mapping; non-unity ratio must not desync homing. Added as a capability + a constraint + a clause in the success signal.
- **Q2 resolved** — reuse the existing `MAX_DRIFT_PPM_DEFAULT = 100` gate as the correctable bound; no new limit. Moved from open question into the bounded-correction constraint.
- **Q3 resolved** — effect verified empirically via unit tests + a bench run (not a runtime closed loop); fail-loud stays open-loop at command time. Open question replaced with a settled assumption.
- **Host-only confirmed** — assumption promoted to confirmed.
- **Q1 (PC8 bug)** — stays a non-goal + the single remaining open question, to revisit only if it surfaces during implementation.

Capability count now 4. Still no companions. Both validation passes hold after the edits.
