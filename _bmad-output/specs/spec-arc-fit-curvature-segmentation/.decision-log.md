# Decision log — spec-arc-fit-curvature-segmentation

## 2026-06-24 — create

Input: an in-session investigation + party-mode brainstorm. The bench crashes (`PieceStartInPast`) on the cube short gcode because the fitter shatters the circle/fillets into a clothoid forest; real-code instrumentation pinned two defects (greedy non-co-circular growth; fragile spiral seating); the brainstorm converged on **curvature-profile segmentation** as the fix. User directive: "let's spec curvature-profile segmentation."

Mode: express (rich input, no elicitation needed).

### Self-validate

**Pass 1 — Coherence.** 3 capabilities, each with intent + testable success (the `arc_fit_neptune` reds, a co-circular fixture test, window-stability across caps). Intents are WHAT (recognize curves as arcs; reliable reconstruction; window-stable classification); the chosen mechanism (curvature segmentation) lives in Why + the companion, and the constraints encode the properties that select it (G2 rules out biarc; near-linear rules out the current O(n²); no-corner-absorption; deviation bound). Five non-goals fence the adjacent work (arc-fit-disabled continuity, default-on config, re-plan amplification, biarc/native-arc, the done seam fix). Success signal is concrete and demonstrable. Lean throughout. Pass.

**Pass 2 — Preservation.** Load-bearing claims all landed: the empirical census (0/≤7 arcs), the two defects with the L1–L51 trace and the 491/138 seating rejects, `cocircular_tol` ruled out as limiter, the curvature-segmentation mechanism and why it dissolves both defects + gives window-stability + kills O(n², Red's three acceptance guards (deviation bound, no corner absorption, idempotence), the streaming-drain connection, and the code map — all in `failure-analysis.md`, cited by the kernel. The κ-noise make-or-break is captured as an assumption; the deviation tolerance, default-on decision, and minimum feature size as open questions. Wrapper-only content (party-mode persona framing, the alternative approaches biarc/split-merge that were considered and not chosen) intentionally dropped — process, not contract; the biarc/split-merge rejection rationale is preserved compactly via the G2 / window-stable / near-linear constraints that exclude them.

Verdict: ready for development pending the three open questions (none block prototyping CAP-1; they bind acceptance thresholds and the default-on decision).

## 2026-06-24 — update (open questions resolved)

User resolved two of three: **arc-fit stays disabled by default for now** (was Q2) → folded into the "production default" non-goal. **Minimum feature size = 3 straight lines** (= 2 turning junctions), and **make it configurable** (was Q3) → new CAP-4: the arc-fit config gains a third parameter (minimum run facets, default 3) exposed via `with_arc_fit` and the host knob; `min_run_junctions` already exists internally and just needs exposing. One open question remains: the chord-deviation acceptance tolerance. Capability count now 4; IDs stable.

## 2026-06-24 — update (config reshape)

User observed the co-circular/deviation tolerance replaces the old angle + distance knobs. Confirmed and folded in: under curvature segmentation, `facet_len_max_mm` (distance) is subsumed (long facet = low curvature = Line) and `max_turn_rad` (angle) is subsumed (sharp corner = single-vertex spike below `min_run`). The remaining config is `{deviation_tol, min_run}` — CAP-4 reframed from "add a third parameter" to "two knobs, the other two removed." Bonus folded into Constraints: the same `deviation_tol` separates line/arc/corner (within-tol-of-line → Line), so no curvature-floor knob is needed. The single remaining open question (deviation tolerance value) is now even more load-bearing — it is the sole quality knob. IDs stable.
