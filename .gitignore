# development
.cache/
out
*.so
*.pyc
__pycache__/
/.config
.config.old
.config.*.*
klippy/.version
.history/
.DS_Store
ci_build/
ci_cache/
_test_.*
klippy/plugins/*
!klippy/plugins/__init__.py
.vscode
.idea/*

# docs
site/
.env/
.venv/
env/
venv/
mkdocs-env/

# fitter prototype run outputs
scripts/fitter_prototype/corpus_results/
# local-only g-code corpora — large slicer dumps live here for manual fitter
# runs; CI test corpora are generated in-test, never committed (see
# rust/compat/tests/corpus.rs and rust/gcode/tests/golden_corpus_lex.rs).
scripts/fitter_prototype/corpus/

# git worktrees
.worktrees/
.claude/worktrees/

# claude-code session-local runtime state (lock file holds session pid +
# acquired-at; the rest of .claude/ — plugins, skills, settings — IS tracked)
.claude/scheduled_tasks.lock

# MCU firmware build outputs
firmware/

# Built artifacts (motion-engine .so, chelper .so) — built per-platform,
# never checked in. Keep klippy/ clean across macOS/Linux/Pi dev.
klippy/_motion_engine*.so
klippy/motion_engine*.dylib
klippy/chelper/c_helper*.so
klippy/chelper/c_helper*.so.dSYM/
klippy/chelper/*.dylib

# Sim-only third-party plugin symlinks (created by tools/sim_klippy/conftest.py)
klippy/extras/beacon.py
klippy/extras/motors_sync.py
klippy/extras/autotune_tmc.py
klippy/extras/motor_constants.py

# Third-party plugin sources (fetched on demand by tools/sim_klippy/fetch_plugins.sh)
tools/sim_klippy/printer_real/third_party_repos/

# Local observability test rig (VictoriaLogs + Vector binaries + scratch data)
.tools/

# Local test-bench notes (passwords, mDNS hostnames, board-specific quirks).
# Not checked in — they describe physical hardware on a specific desk.
test-benches/

graphify-out/
